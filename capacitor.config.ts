import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.suhinin.app',
  appName: 'InnoTreker',
  webDir: 'dist/spa',
  bundledWebRuntime: false
};

export default config;
